[project]
name = "webdigestor"
version = "0.0.0"
description = "Add your description here"
authors = [
    { name = "atayarani", email = "619512+ChronoSerrano@users.noreply.github.com" }
]
dependencies = []
readme = "README.md"
requires-python = ">= 3.10"

#[project.scripts]
#hello = "webdigestor:hello"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = []

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/webdigestor"]
