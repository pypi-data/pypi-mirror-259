[tool.poetry]
name = "vijil-cli"
version = "0.1.15"
description = "Vijil CLI for interacting with Vijil"
authors = ["Vijilai <your.email@example.com>"]

[tool.poetry.dependencies]
python = "^3.9"
requests = "^2.26.0"
python-dotenv = "^1.0.0"
click = "*"
tabulate = "*"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
vijil = "vijil_cli.cli:main"
