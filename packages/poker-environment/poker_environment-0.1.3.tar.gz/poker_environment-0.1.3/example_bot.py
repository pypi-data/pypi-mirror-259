from poker_environment import PyPokerEnvironment, call


"""
class PyPokerEnvironment {
    table_cards: list[str]
    current_player: {
        hand: list[str]
        remaining_balance: int
        committed_balance: int
    },
    player_states: list[{
        remaining_balance: int
        committed_balance: int
    }]
    game_history: list[list[str]]
}
"""
def execute(environment: PyPokerEnvironment):
    return call()
