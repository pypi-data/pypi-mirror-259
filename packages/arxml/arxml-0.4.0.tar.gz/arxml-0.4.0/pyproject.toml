[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "arxml"
version = "0.4.0"
description = "Python AUTOSAR XML parser"

requires-python = ">=3.11"
readme = "README.rst"
license = {file = "LICENSE"}
