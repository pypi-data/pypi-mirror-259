from setuptools import setup, find_packages

setup(
    name='Pila-Project',
    version='0.2.0',
    packages=find_packages(),
    install_requires=[
        # Lista de dependencias si las hay
    ],
)