from setuptools import setup, find_packages

setup(
    name='bug_tracker',
    version='1.3.3',
    packages=find_packages(),
    entry_points={
        'console_scripts': [
            # Add any command-line scripts here
        ],
    },
)
