[tool.poetry]
name = "parallelexec"
version = "0.0.3"
description = "Module that provides decorators and methods to easily run multiple functions concurrently"
authors = ["AshGw <AshrefGw@proton.me>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
