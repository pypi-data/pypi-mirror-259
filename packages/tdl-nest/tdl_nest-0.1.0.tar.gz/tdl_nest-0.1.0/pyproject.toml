[tool.poetry]
name = "tdl-nest"
version = "0.1.0"
description = "simple library to iterate through nested dict, list, and tuples"
authors = ["Daniel L Marino <daniellml55@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
