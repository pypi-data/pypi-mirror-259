from setuptools import setup, find_packages

setup(
    name='straightemail',
    version='0.0.1',
    author="Shreejeet Sahay",
    packages=find_packages(),
    install_requires=[
    ]
    )
