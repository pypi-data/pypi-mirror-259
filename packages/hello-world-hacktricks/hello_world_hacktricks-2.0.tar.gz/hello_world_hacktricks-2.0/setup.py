# setup.py
from setuptools import setup, find_packages

setup(
    name='hello_world_hacktricks',
    version='2.0',
    packages=find_packages(),
    install_requires=[
        # Any dependencies your library needs
    ],
)
